apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: astraops-worker-hpa
  namespace: astraops
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: astraops-worker
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
